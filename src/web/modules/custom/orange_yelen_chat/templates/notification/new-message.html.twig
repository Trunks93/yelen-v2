<!doctype html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>{{ site_name }} - Nouveau message dans le Tchat</title>
  <style>
    /* Styles pour assurer la compatibilité avec les clients mail */
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      line-height: 1.6;
      background-color: #f4f4f4;
    }
    .email-wrapper {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      margin-top: 2rem;
    }
    .header {
      padding: 20px;
      text-align: center;
      background-color: #ffffff;
    }
    .logo {
      max-width: 100px;
      height: auto;
    }
    .content {
      padding: 30px;
      background-color: #ffffff;
      color: #333333;

      .greeting{
        font-size: 1.5rem;
      }
    }
    .footer {
      padding: 20px;
      text-align: center;
      background-color: #f8f8f8;
      color: #666666;
      font-size: 12px;
    }
    @media only screen and (max-width: 600px) {
      .email-wrapper {
        width: 100% !important;
      }
      .content {
        padding: 20px !important;
      }
    }
  </style>
</head>
<body>
<div class="email-wrapper">
  <div class="header">
    <img src="{{ site_logo }}" alt="{{ site_name }}" class="logo">
  </div>

  <div class="content">
    <h1 class="greeting">{{ content.greeting }} <strong>{{ content.recipient_name }}</strong>,</h1>
    <p>Vous avez une reçu un nouveau message dans le Tchat.</p>
    <p>Connectez-vous à votre compte pour le consulter.</p>
    <p>Merci !</p>
  </div>

  <div class="footer">
    {{ footer_text }}
    <p>© {{ "now"|date("Y") }} {{ site_name }}. Tous droits réservés.</p>
  </div>
</div>
</body>
</html>
