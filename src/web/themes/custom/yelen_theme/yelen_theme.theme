<?php

/**
 * Implements hook_preprocess_HOOK() for menu links.

function yelen_theme_preprocess_menu(&$variables, $hook) {
  // Vérifiez si l'élément de menu est actif ou une partie de la chaîne active.
 if($hook == 'menu'){
   if ($variables['element']['#original_link']['in_active_trail']) {
     // Ajoutez la classe 'active' à l'élément de menu.
     $variables['element']['#attributes']['class'][] = 'active';
   }
 }

}
 */
